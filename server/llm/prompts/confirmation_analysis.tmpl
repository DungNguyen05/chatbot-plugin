You are analyzing whether a user confirmed or denied a previous request.

Previous confirmation message: "{{.Parameters.ConfirmationMsg}}"
{{if .Parameters.OriginalIntent}}
Original intent: {{.Parameters.OriginalIntent.Category}} - {{.Parameters.OriginalIntent.Action}}
{{end}}
User's response: "{{.Parameters.UserMessage}}"

Analyze the user's response to determine if they:
1. CONFIRMED (agreed/accepted) the action
2. DENIED (disagreed/rejected) the action

Consider these patterns but don't rely only on keywords:

CONFIRMATION indicators (various languages):
- Vietnamese: có, đúng, được, ok, okay, ừ, ừm, uhm, xác nhận, đồng ý, chính xác, đ, uh huh, vâng, ạ
- English: yes, yeah, yep, sure, okay, ok, correct, right, confirm, absolutely, definitely, y, uh-huh
- Phrases: "làm đi", "ghi nhận đi", "đúng rồi", "chính xác", "go ahead", "do it", "that's right"

DENIAL indicators:
- Vietnamese: không, sai, hủy, thôi, ko, k, bỏ, chưa, chờ, dừng lại
- English: no, nope, nah, wrong, cancel, stop, wait, not yet, incorrect, deny
- Phrases: "không phải", "sai rồi", "that's wrong", "not what I meant", "cancel that"

IMPORTANT: Use context and meaning, not just keyword matching. Consider:
- Tone and context of the response
- Whether they're correcting or clarifying
- If they're asking questions vs confirming
- Body language expressions in text (emoji, punctuation)

Respond with ONLY valid JSON:
{
  "confirmed": true/false,
  "reasoning": "Brief explanation of why you determined this"
}